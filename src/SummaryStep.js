import React from "react";
import {
  Container,
  Divider,
  Grid,
  Input,
  Button,
  List,
  Icon,
  Transition,
} from "semantic-ui-react";

const SummaryStep = (props) => {
  return (
    <React.Fragment>
      <Container>
        <List>
          <List.Item>Summary Step here</List.Item>
        </List>
      </Container>
    </React.Fragment>
  );
};

export default SummaryStep;
